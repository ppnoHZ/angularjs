/** * Created by ID on 15/11/10. * Author:zhoudd * email:zhoudd@stark.tm * * 可以手动进行表达式运行，如加减乘除。 *  类似javascript 中的 eval(1+1)  //2 *  $watch 监视某个属性的变化 * *  */var app = angular.module('app', []);app.controller('appController', ['$scope', '$interpolate', function ($scope, $interpolate) {    //    $scope.to = 'zhoudd@stark.tm';    $scope.emailBody = 'Hello {{ to }},\n\nMy name is Ari too!';  //    $scope.$watch('emailBody', function (body) {            if (body) {                var template = $interpolate(body);                $scope.previewText =                    template({to: $scope.to});            }        }    )}])