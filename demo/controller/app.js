/** * Created by ID on 15/11/10. * Author:zhoudd * email:zhoudd@stark.tm */var app = angular.module('app', [])app.controller('ParentController', function ($scope) {    $scope.person = {greeted: false};    $scope.p = {name: "zhoudd"};//不能直接写 $scope.p.name='z' 来赋值    $scope.name = "ddd";//如果这样赋值想再  child中修改是行不通的，});app.controller('ChildController', function ($scope) {    $scope.sayHello = function () {        //首先在当前作用域中查找要设置的属性，如果没有就会到上层scope中查找        $scope.person.greeted = true;        $scope.person.name = "Child Change to zhoudd";        $scope.p.name = "Child Change to zhoudd";//子控制器改变父控制器的p对象的值        $scope.name = "111";//无效的修改，只会在child中添加一个name属性，不会对父的name属性造成影响    }});