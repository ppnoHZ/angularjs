/** * Created by ID on 15/11/12. * Author:zhoudd * email:zhoudd@stark.tm */angular.module('note', ['noteRouter', 'ngStorage', 'app.Services'])    .config(function ($httpProvider) {        $httpProvider.defaults.useXDomain = true;    })    .controller('HomeController', function ($scope, $http, note_Services) {        note_Services.getAllNotes({}).success(            function (data) {                $scope.notes = data.notes;            }        )        $scope.submitForm = function () {            note_Services.addNote({text: $scope.text})                .success(function (data) {                    if (data.status != 'success') {                        alert(data);                    }                })        }    })    .controller('NavbarControler', function ($scope, $window, UserService, $location) {        $scope.token = $window.localStorage.getItem('token');        $scope.submitForm = function () {            if (!$scope.userName || !$scope.password) {                return;            }            UserService.login({                username: $scope.userName,                password: $scope.password            }).success(function (data) {                if (data.status != 'success') {                    alert(data.status);                    return;                }                console.log('login', data);                $window.localStorage.setItem('token', data.token);                $location.path('/home');            })        }    })    .controller('RegisterController', function ($scope, $location,                                                $http,                                                $localStorage,                                                $window,                                                $sessionStorage, UserService) {        $scope.submitForm = function () {            if (!$scope.userName || !$scope.password || !$scope.password2 || !$scope.nickName) {                return;            } else {                if ($scope.password != $scope.password2) {                    return;                }            }            UserService.register({                username: $scope.userName,                password: $scope.password,                nickName: $scope.nickName            }).success(function (data) {                $window.localStorage.setItem('token', data.token);                $location.path('/home');            }).error(function (error) {                console.log('error', error);            })        }    })